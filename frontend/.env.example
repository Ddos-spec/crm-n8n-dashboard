# ==============================================
# CRM Frontend Environment Configuration
# ==============================================
# For SINGLE VPS DEPLOYMENT:
# - You DON'T need this file when deploying to same VPS
# - Frontend will use /api as base URL (relative path)
# - The backend serves the static frontend files
#
# For SEPARATE DEPLOYMENT only:
# - Copy this to .env
# - Set the backend API URL below
# ==============================================

# Backend API Base URL
# For same-VPS: Leave this commented out (uses relative /api)
# For separate deployment: Uncomment and set your backend URL
# REACT_APP_API_BASE_URL=https://your-backend-domain.com/api
