// ALTERNATIVE CORS CONFIG - Use this if whitelist approach doesn't work
// This allows ALL origins - USE ONLY FOR DEBUGGING

const corsOptionsDebug = {
  origin: '*', // Allow all origins
  credentials: false, // Must be false when origin is *
  methods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS', 'PATCH'],
  allowedHeaders: ['Content-Type', 'Authorization', 'X-Requested-With', 'Accept'],
  exposedHeaders: ['Content-Range', 'X-Content-Range'],
  maxAge: 86400
};

// Replace line 41 with:
// app.use(cors(corsOptionsDebug));

// ⚠️ WARNING: This is NOT secure for production!
// After confirming CORS works, switch back to whitelist approach
