# ==============================================
# CRM Backend Environment Configuration
# ==============================================
# For SINGLE VPS DEPLOYMENT (Frontend + Backend on same server)
#
# IMPORTANT:
# - Copy this file to .env and update the values
# - For same-VPS deployment, you can omit CORS_ORIGIN or set it to *
# - All sensitive values below are EXAMPLES - generate your own!
# ==============================================

# Application
NODE_ENV=production
PORT=4444

# Public URL (your domain or VPS IP)
PUBLIC_URL=https://your-domain.com/

# Database Connection
# Replace with your actual PostgreSQL credentials
DATABASE_URL=postgres://username:password@localhost:5432/CRM?sslmode=disable

# JWT Secret (generate a secure random string)
# You can generate one with: openssl rand -hex 64
JWT_SECRET=your-secure-jwt-secret-here-generate-a-long-random-string

# CORS Configuration
# For same-VPS setup: omit this line or set to *
# For separate deployment: set to your frontend URL
# CORS_ORIGIN=*
# CORS_ORIGIN=https://your-frontend-domain.com

# WhatsApp Integration
WHATSAPP_API_URL=https://app.notif.my.id/api/v2/send-message
WHATSAPP_API_KEY=your-whatsapp-api-key-here